sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Embedding stack pin: transformers (pulled by sentence-transformers) requires
# huggingface-hub<1.0, so pin it here. Gradio prefers a newer hub but only needs
# it for Spaces features we don't use; ChatInterface works fine on this version.
huggingface-hub>=0.34.0,<1.0

# Ingestion: HTML -> Markdown
requests>=2.31.0
beautifulsoup4>=4.12.0
markdownify>=0.13.0
lxml>=5.0.0

# Query interface (Milestone 5)
gradio>=6.9.0
